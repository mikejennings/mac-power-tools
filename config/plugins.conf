# Mac Power Tools Plugin Configuration
# Format: plugin_name=enabled|disabled

# Core plugins (always enabled)
help=enabled
version=enabled

# Optional plugins
battery=enabled
network=enabled
disk=enabled
memory=enabled
update=enabled
info=enabled
maintenance=enabled
shortcuts=enabled
awake=enabled
